import { DurableObject } from "cloudflare:workers";
export class GlobalDurableObject extends DurableObject {
  /**
   * GlobalDurableObject maintained for future stateful requirements.
   * Legacy demo methods removed to align with production hardening.
   */
}